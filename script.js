const booksLibrary = [];

function Book(title,pages,status,author){
    this.title = title,
    this.pages = status,
    this.auther = author,
    this.status = status;
}

function addBookToLibrary(){
    
}